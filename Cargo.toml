[package]
name = "osal-rs"
version = "0.1.0"
edition = "2021"
authors = ["passy1977"]
description = "Operating System Abstraction Layer for POSIX and FreeRTOS"
license = "MIT"
repository = "https://github.com/passy1977/osal-rs"

[features]
default = ["posix"]
posix = []
freertos = []

[dependencies]
# FreeRTOS bindings would be added when implementing FreeRTOS backend
# freertos-rust = { version = "0.3", optional = true }

[dev-dependencies]
