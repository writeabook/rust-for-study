//! Legacy test modules — kept as reference; no longer compiled.
//!
//! The standalone `linux` backend has been removed.  Linux host testing
//! is now covered by the POSIX backend via `cargo test --features posix`.
